using UnityEngine;
using System.Collections;

public class DispPos : MonoBehaviour {

	Vector3 p = new Vector3(-1,-1,-1);

	// Update is called once per frame
	void Update () {
		//Get base marker transformation -> view matrix (transformation to camera position
		
		//Get this markers position

		Debug.Log (p);
	}
}
